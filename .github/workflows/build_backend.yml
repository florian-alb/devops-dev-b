name: Build & Deploy BACKEND to Scaleway
on:
  push:
    branches:
      - dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Login to Scaleway Container Registry
        uses: docker/login-action@v3
        with:
          username: nologin
          password: ${{ secrets.SCW_SECRET_KEY }}
          registry: ${{ secrets.CONTAINER_REGISTRY_ENDPOINT }}

      - name: Build the Docker image
        working-directory: back
        run: docker build . -t ${{ secrets.CONTAINER_REGISTRY_ENDPOINT }}/backend-image

      - name: Push the Docker Image
        working-directory: back
        run: docker push ${{ secrets.CONTAINER_REGISTRY_ENDPOINT }}/backend-image

  deploy:
    needs: build
    runs-on: ubuntu-latest
    name: Deploy on Scaleway Containers
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use CLI
        uses: scaleway/action-scw@v0
        with:
          save-config: true
          export-config: true
          version: v2.24.0
          access-key: ${{ secrets.SCW_ACCESS_KEY }}
          secret-key: ${{ secrets.SCW_SECRET_KEY }}
          default-project-id: ${{ secrets.SCW_DEFAULT_PROJECT_ID }}
          default-organization-id: ${{ secrets.SCW_DEFAULT_ORGANIZATION_ID }}

      - name: List Scaleway Namespaces
        run: |
          echo "Listing Scaleway namespaces..."
          scw container registry namespace list

      - name: Create a container
        run: |
          scw container container create name=container-backend \
          namespace-id=b48809e1-e0a8-47f6-b3a7-b52abd3a8fce \
          registry-image=${{ secrets.CONTAINER_REGISTRY_ENDPOINT }}/backend-image:latest \
          min-scale=0 \
          max-scale=1 \
          memory-limit=120 \
          cpu-limit=100 \
          port=3000 \
          environment-variables.JWT_SECRET=${{ secrets.JWT_SECRET }} \
          environment-variables.DATABASE_URL=${{ secrets.DATABASE_URL }}
